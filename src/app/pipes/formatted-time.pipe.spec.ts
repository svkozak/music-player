import { FormattedTimePipe } from './formatted-time.pipe';

describe('FormattedTimePipe', () => {
  it('create an instance', () => {
    const pipe = new FormattedTimePipe();
    expect(pipe).toBeTruthy();
  });
});
